{
  "name": "mern-template",
  "version": "1.0.0",
  "description": "Template MERN avec Vite, Playwright, Atomic Design, Zod, ESLint Airbnb, Prettier",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "lint": "npm run lint --workspace=client && npm run lint --workspace=server",
    "lint:fix": "npm run lint:fix --workspace=client && npm run lint:fix --workspace=server",
    "lint:files": "ls-lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run test --workspace=client",
    "test:e2e": "npm run test:e2e --workspace=client",
    "prepare": "husky",
    "check-all": "npm run lint && npm run lint:files && npm run format:check"
  },
  "devDependencies": {
    "@commitlint/cli": "^18.6.0",
    "@commitlint/config-conventional": "^18.6.0",
    "@ls-lint/ls-lint": "^2.2.3",
    "concurrently": "^8.2.2",
    "husky": "^9.0.10",
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
