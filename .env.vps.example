# ===========================================
# VARIABLES D'ENVIRONNEMENT VPS
# ===========================================
# Copier ce fichier en .env et remplir les valeurs

# Domaine principal (sans https://)
DOMAIN=monapp.com

# Email pour les certificats SSL Let's Encrypt
ACME_EMAIL=ton-email@example.com

# ===========================================
# MongoDB Atlas
# ===========================================
MONGODB_URI_DEV=mongodb+srv://dev-user:PASSWORD@cluster.mongodb.net/myapp-dev
MONGODB_URI_PROD=mongodb+srv://prod-user:PASSWORD@cluster.mongodb.net/myapp-prod

# ===========================================
# JWT Secrets (générer avec: openssl rand -base64 32)
# ===========================================
JWT_SECRET_DEV=change-me-dev-secret
JWT_SECRET_PROD=change-me-prod-secret-different
